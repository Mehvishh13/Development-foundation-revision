######## [REPL commands] #########

[REPL: Read, Evaluate, Print, Loop]

  REPL is a simple interactive programming environment that takes single user inputs, executes them, and returns the result to the user. It is commonly used for testing and debugging code snippets in real-time. In Node.js, you can access the REPL by simply typing `node` in your terminal without any arguments. This allows you to execute JavaScript code directly and see the results immediately.

.break->  Sometimes you get stuck, this gets you out of the loop
.clear    Alias for .break
.editor   Enter editor mode
.exit     Exit the REPL
.help     Print this help message
.load     Load JS from a file into the REPL session
.save     Save all evaluated commands in this REPL  session to a file



-----------------------------------------------------

############## {EXPORT AND IMPORT} #############3

! Suppose we have (A.js) file containing some functions and variables and we want to use that in some other file (B.js)  ==> write {require("A")} in B.js and we write {export syntax} in first file



################{WriteHead second parameter}####################

####Content-Type--> defines the media type of the response content. 
for example-->  res.setHeader('Content-Type','application/json') or 

res.setHeader('Content-Type','text/html')
res.setHeader('Content-Type','text/plain').



####Cache-Control(Control Browsing Caching)--> controls how browser cache  the response


####Content-Disposition (Force File Download)--> use to suggest a file download.

####Cors Headers(cross-origin resource sharing)--. allows response to be accessed from different origins.

for example->  res.setHeader('Access-Control-Allow-Origin','*'); ---> here * means we allow request from all domains.